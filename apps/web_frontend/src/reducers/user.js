const initialState = {
  name: '',
  email: '',
  pic: '',
  isSignedIn: false,
  tok: ''
}

export default function UserReducer (state = initialState, action) {
  switch (action) {
    default:
      return state
  }
}
